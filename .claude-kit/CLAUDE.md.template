# CLAUDE.md — pemuat aturan tim <NAMA_PROYEK> (otomatis dibaca Claude Code tiap sesi)

> File ini sengaja kecil. Tugasnya **cuma memuat** aturan dari 2 sumber di bawah —
> JANGAN tulis aturan langsung di sini.
>
> 1. **Kernel aturan tim** (`AGENTS.md`) — sumber tunggal universal, dibaca native oleh Codex/Kimi/Cursor
>    juga. Milik kit, di-refresh lewat "update lintasAI" — JANGAN edit tangan.
> 2. **Override khusus proyek** (`AGENTS.override.md`) — punya kamu, tak pernah ditimpa saat update.
>
> Kalau Claude Code menampilkan dialog "izinkan impor file?" di sesi pertama, klik **Allow**
> (itu cuma menarik isi 2 file di bawah ke konteks). Kalau dialog tidak muncul, abaikan.
>
> **Baca `.claude-kit/PETA.md` dulu** untuk tahu "apa di mana" di dalam kit (fungsi tiap folder +
> daftar skill) + ke mana menaruh berkas baru. PETA dirujuk (bukan disalin) supaya tak basi.

@./AGENTS.md
@./AGENTS.override.md
